---
import "../styles/global.css";
---
<!doctype html>
<html lang="vi" class="h-full">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Admin</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body class="min-h-screen bg-slate-950 text-slate-200">
    <div class="flex min-h-screen">
      <aside class="hidden md:block w-64 bg-slate-900 border-r border-slate-800">
        <div class="p-4 text-lg font-semibold">Admin</div>
        <nav class="px-2 space-y-1">
          <a class="block px-3 py-2 rounded hover:bg-slate-800" href="/">Trang chủ</a>
          <a class="block px-3 py-2 rounded hover:bg-slate-800" href="/redirects">Redirects</a>
          <a class="block px-3 py-2 rounded hover:bg-slate-800" href="/products">Products</a>
        </nav>
      </aside>
      <div class="flex-1">
        <header class="sticky top-0 z-10 bg-slate-900/80 backdrop-blur border-b border-slate-800">
          <div class="mx-auto max-w-6xl px-4 py-3 flex items-center justify-between">
            <div class="text-base md:text-lg font-semibold">Admin Panel</div>
            <div class="flex items-center gap-2">
              <a class="btn" href="/">Dashboard</a>
            </div>
          </div>
        </header>
        <main>
          <div class="mx-auto max-w-6xl px-4 py-6">
            <slot />
          </div>
        </main>
      </div>
    </div>
  </body>
  </html>
